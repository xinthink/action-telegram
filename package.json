{
  "name": "action-telegram",
  "version": "1.0.0",
  "private": true,
  "description": "Telegram notification for workflow set up with GitHub Actions",
  "main": "lib/main.js",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "clean": "rm -rf lib",
    "rebuild": "yarn clean && yarn build",
    "release": "yarn && yarn rebuild && rm -rf node_modules && yarn --prod"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/xinthink/action-telegram.git"
  },
  "keywords": [
    "actions",
    "node",
    "Telegram"
  ],
  "author": "xinthink",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.0.0",
    "@actions/github": "^1.0.0",
    "request": "^2.88.0",
    "request-promise-native": "^1.0.7"
  },
  "devDependencies": {
    "@types/jest": "^24.0.13",
    "@types/node": "^12.0.4",
    "@types/request-promise-native": "^1.0.16",
    "jest": "^24.8.0",
    "jest-circus": "^24.7.1",
    "ts-jest": "^24.0.2",
    "typescript": "^3.5.1"
  }
}
